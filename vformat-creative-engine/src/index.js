import React from 'react';
import ReactDOM from 'react-dom/client';
import VFormatCreativeEngine from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <VFormatCreativeEngine />
  </React.StrictMode>
);
